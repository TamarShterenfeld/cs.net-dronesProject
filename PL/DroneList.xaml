<Window x:Class="PL.DroneList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="DroneList" Height="450" Width="800">
    <Grid>
        <!--<Grid.RowDefinitions >
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>-->
        <Grid >
            <ListView x:Name ="DroneListView" Grid.Row="0" >
                <ListView.View>
                    <GridView>
                        <GridViewColumn/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
        <Label x:Name="Filter" Content="choose category:" HorizontalAlignment="Left" Margin="240,80,0,0" VerticalAlignment="Top" Width="120" Grid.Column="0"/>

        <ComboBox x:Name="FilterComboBox" ItemsSource="{Binding}" HorizontalAlignment="Left" Margin="350,80,0,0" VerticalAlignment="Top" Width="120" Grid.Column="1" SelectionChanged="StatusSelector_SelectionChanged">
           
        </ComboBox>
        
        
    </Grid>
</Window>
